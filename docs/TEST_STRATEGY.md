# Test strategy

## Release 方針

Local File Transfer は、browser file provider、local HTTP、Electron process、SQLite、Windows file semantics、dynamic network adapter、responsive UI をまたぎます。単一の test layer だけでは十分な confidence を得られません。

RC.4 は unit test、fault injection、real Fastify integration、browser/Electron E2E、DPI geometry、packaged process test、artifact attestation、physical-device manual gate を組み合わせます。

## Automated layer

### Unit と contract test

- base64url、hash、range、fingerprint、checkpoint/idempotency math
- Runtime protocol validation、ID、revision、Unicode、byte limit
- SpecQR error-correction selection
- 日本語・英語 catalog completeness
- Resume store と file-source materialization
- Shared text cryptography と key zeroization
- Adapter ranking、request guard、connection gate、log redaction

### Server integration と fault test

- Room creation、authorization、ticket cookie、exact Origin/Host rule
- Upload registration、`HEAD`/`PATCH` offset、checksum、duplicate commit、stale offset
- 15 MiB 以上の browser-upload regression path
- SQLite migration、WAL recovery、partial-file truncate/offset rewind、retention、cleanup
- Write/fsync/SQLite update/commit/ACK loss の fault phase と idempotent completion recovery
- Service restart、simulated write failure、source change、disk constraint、cancellation
- Range、`If-Range`、`416`、streaming ZIP、archive source fault
- Shared text conflict、expiry、restart、SQLite/WAL の plaintext absence

### Browser と Electron E2E

Built app を起動し、2 つの mobile profile を検証します。

- iPhone-sized WebKit
- Android-sized Chromium

各 scenario は authorization、file flow の両方向、queue state、両 endpoint からの Shared text、unread state、conflict、IME guard、reload convergence、offline/online recovery、reset、action reachability を確認します。

目的は mock protocol だけではなく、build 済み application の behavior を検証することです。

### Visual geometry

Windows scale 100%、125%、150%、200% で次を検証します。

- Horizontal overflow がない。
- QR region が正方形で viewport 内に収まる。
- Dialog が window 内に収まる。
- Desktop width が state transition で変わらない。
- Queue 0-3 row で window が適切に伸縮する。
- 4 row 以上では queue 内部だけが scroll する。
- User が手動で広げた height を自動で縮めない。

### Packaged verification

x64 Portable smoke は development server ではなく生成した EXE を `--disable-gpu` で使用します。Launch、health、Utility Process restart、graceful close、endpoint shutdown、residual process count を確認します。GPU/rendering と DPI は別の visual geometry gate が担当します。

PE header と Electron fuse は両 architecture で確認します。ARM64 runtime は物理 device を必要とする別 gate です。

## RC.4 release gate

- Unit/integration/release-script test: clean run required
- Android Chromium と iPhone WebKit E2E: clean run required
- x64 packaged launch/service-recovery/shutdown smoke: required
- x64/ARM64 build、PE、fuse、audit、SBOM、static check: required
- Build provenance と SBOM attestation の `gh attestation verify`: required
- ARM64 physical runtime: not performed unless separately recorded

Exact count と artifact-specific result は tag workflow が生成する `docs/release/2.0.0-rc.4` 相当の release evidence に記録します。RC.2 の checked-in evidence を current result として再利用しません。

## Manual release gate

[MANUAL_TEST_CHECKLIST.md](MANUAL_TEST_CHECKLIST.md) を次の組み合わせで実施します。

- Windows x64 + iPhone Safari
- Windows x64 + Android Chrome
- Windows x64 + 別 desktop browser
- ARM64 runtime claim を行う場合は physical Windows on ARM

重点項目:

- Camera で撮影した 15 MiB 以上の JPEG/HEIC
- Multiple files
- Screen lock と backgrounding
- Wi-Fi interruption
- Page reload と file re-selection による resume
- Download destination
- Windows Firewall prompt
- Adapter change
- Unicode/duplicate name
- Low disk space
- Cancellation と cleanup

## Test data policy

Generated byte と synthetic name だけを使用します。Personal photo、document、log、database、live room QR screenshot、local path、transfer artifact を commit しないでください。

Large fixture は test 中に生成し、cleanup で削除します。Evidence 自体が publication surface になるため、redaction も automated test の対象です。
